<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rodial - Digital Twin Network Research</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <!-- Chosen Palette: Cool Neutrals & Tech Blue -->
    <!-- Application Structure Plan: The application is designed as a single-page, multi-section dashboard to provide a comprehensive yet digestible overview of the Rodial concept. This structure was chosen because the source material contains distinct but related ideas: the core architecture, a new ML model, and performance scalability. A thematic section-based approach (Intro -> Architecture -> ML Model -> Performance Sim) guides the user logically from the high-level concept to technical details and performance implications. This is more intuitive than a linear report, allowing users to focus on the aspect most relevant to them. Key interactions include clicking diagram components for details, selecting ML parameters, and using a slider to visualize performance changes, fostering active exploration. -->
    <!-- Visualization & Content Choices: 
        - Report Info: System flow from user's image. Goal: Organize/Relate. Viz: Interactive diagram using HTML/CSS. Interaction: Click components to reveal detailed text. Justification: A static image is non-interactive; this makes the complex architecture explorable. Method: HTML divs with Tailwind CSS.
        - Report Info: ML model controlling network bands. Goal: Inform/Interact. Viz: Control panel with buttons and a status display, supported by a Bar Chart. Interaction: User clicks a band (2.4/5/6GHz) to simulate ML selection, updating the chart and status. Justification: This transforms an abstract idea into a tangible, interactive simulation of the model's function. Library: Chart.js for the bar chart.
        - Report Info: Performance under 3 to 1000 devices. Goal: Compare/Show Change. Viz: Line Chart showing Latency vs. Throughput. Interaction: User moves a slider for device count, which dynamically updates the chart. Justification: A dynamic chart is the most effective way to show the relationship between device load and network performance. Library: Chart.js.
    -->
    <!-- CONFIRMATION: NO SVG graphics used. NO Mermaid JS used. -->
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
            color: #1e293b;
        }
        .chart-container {
            position: relative;
            width: 100%;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            height: 300px;
        }
        @media (min-width: 768px) {
            .chart-container {
                height: 400px;
            }
        }
        .glassmorphism-card {
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 1rem;
        }
        .diagram-node {
            transition: all 0.3s ease-in-out;
            cursor: pointer;
        }
        .diagram-node:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .diagram-connector {
            position: absolute;
            background-color: #94a3b8;
            z-index: -1;
        }
        .band-button.active {
            background-color: #2563eb;
            color: white;
            box-shadow: 0 4px 14px 0 rgba(37, 99, 235, 0.39);
        }
        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 24px;
            height: 24px;
            background: #2563eb;
            cursor: pointer;
            border-radius: 50%;
        }
        .slider::-moz-range-thumb {
            width: 24px;
            height: 24px;
            background: #2563eb;
            cursor: pointer;
            border-radius: 50%;
        }
    </style>
</head>
<body class="antialiased">
    <header class="bg-white/80 backdrop-blur-lg sticky top-0 z-50 border-b border-slate-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center space-x-2">
                     <div class="w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
                        </svg>
                    </div>
                    <h1 class="text-2xl font-bold text-slate-800">Rodial Research</h1>
                </div>
                <nav class="hidden md:flex space-x-8">
                    <a href="#architecture" class="text-sm font-medium text-slate-600 hover:text-blue-600 transition-colors">Architecture</a>
                    <a href="#ml-model" class="text-sm font-medium text-slate-600 hover:text-blue-600 transition-colors">ML Model</a>
                    <a href="#simulation" class="text-sm font-medium text-slate-600 hover:text-blue-600 transition-colors">Performance</a>
                </nav>
            </div>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <section id="intro" class="text-center mb-20">
            <h2 class="text-4xl font-bold tracking-tight text-slate-900 sm:text-5xl">Reimagining Remote Connectivity</h2>
            <p class="mt-6 max-w-3xl mx-auto text-lg leading-8 text-slate-600">
                This research dashboard explores the "Rodial" system, a novel architecture using a network's digital twin to provide full-strength, low-latency internet access to mobile devices, regardless of their proximity to the physical router. We introduce an ML model for intelligent network control and simulate its performance at scale.
            </p>
        </section>

        <section id="architecture" class="mb-24 scroll-mt-20">
            <div class="text-center mb-12">
                <h3 class="text-3xl font-bold text-slate-900">System Architecture</h3>
                <p class="mt-4 max-w-2xl mx-auto text-base text-slate-500">
                    The Rodial system is composed of three core components that work in tandem to create a seamless, high-speed connection. Click on each component in the interactive diagram below to learn more about its specific role and function within the ecosystem.
                </p>
            </div>

            <div class="relative p-8 md:p-12 min-h-[500px] flex flex-col md:flex-row justify-around items-center">
                <div class="diagram-node glassmorphism-card p-6 w-56 text-center" data-info="physical-router">
                    <div class="text-4xl">📡</div>
                    <h4 class="font-semibold mt-2">Physical Router</h4>
                    <p class="text-xs text-slate-500">The Source of Internet</p>
                </div>

                <div class="w-full md:w-auto h-16 md:h-auto flex md:flex-col items-center justify-center my-4 md:my-0 relative">
                    <div class="text-slate-400 text-sm font-medium absolute -top-4 md:top-auto md:-left-20">Initialization (5MB Seed)</div>
                    <svg class="w-6 h-6 text-blue-500 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                </div>
                
                <div class="diagram-node glassmorphism-card p-6 w-56 text-center" data-info="digital-twin">
                     <div class="text-4xl">☁️</div>
                    <h4 class="font-semibold mt-2">Digital Twin</h4>
                    <p class="text-xs text-slate-500">Cloud-Based Network Replica</p>
                </div>

                <div class="w-full md:w-auto h-16 md:h-auto flex md:flex-col items-center justify-center my-4 md:my-0 relative">
                     <div class="text-slate-400 text-sm font-medium absolute -top-4 md:top-auto md:-left-20">VPN Tunnel</div>
                    <svg class="w-6 h-6 text-blue-500 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                </div>

                <div class="diagram-node glassmorphism-card p-6 w-56 text-center" data-info="mobile-device">
                    <div class="text-4xl">📱</div>
                    <h4 class="font-semibold mt-2">Mobile Device</h4>
                    <p class="text-xs text-slate-500">"Rodial" App User</p>
                </div>
            </div>

            <div id="info-box" class="mt-8 min-h-[120px] glassmorphism-card p-6 transition-opacity duration-300 opacity-0">
                <h4 id="info-title" class="font-bold text-lg text-slate-800"></h4>
                <p id="info-text" class="mt-2 text-slate-600"></p>
            </div>
        </section>

        <section id="ml-model" class="mb-24 scroll-mt-20">
             <div class="text-center mb-12">
                <h3 class="text-3xl font-bold text-slate-900">ML-Powered Band Control</h3>
                <p class="mt-4 max-w-2xl mx-auto text-base text-slate-500">
                   An integrated Machine Learning model dynamically analyzes network conditions to take control of the optimal frequency band from the physical router. This ensures the digital twin always operates with the best possible performance. Select a band to simulate the ML model's takeover command.
                </p>
            </div>
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
                <div class="glassmorphism-card p-8">
                    <h4 class="font-semibold text-lg mb-2">Simulate ML Command</h4>
                    <p class="text-sm text-slate-500 mb-6">Choose a band for the Digital Twin to control.</p>
                    <div class="flex space-x-4 mb-6">
                        <button class="band-button flex-1 p-3 rounded-lg font-semibold transition-all duration-200" data-band="2.4">2.4 GHz</button>
                        <button class="band-button flex-1 p-3 rounded-lg font-semibold transition-all duration-200" data-band="5">5 GHz</button>
                        <button class="band-button flex-1 p-3 rounded-lg font-semibold transition-all duration-200" data-band="6">6 GHz</button>
                    </div>
                    <div class="bg-slate-100 p-4 rounded-lg">
                        <p class="text-sm text-slate-500">Current Status:</p>
                        <p id="ml-status" class="text-lg font-semibold text-slate-800">Awaiting ML command...</p>
                    </div>
                </div>
                <div>
                    <div class="chart-container">
                        <canvas id="mlChart"></canvas>
                    </div>
                </div>
            </div>
        </section>
        
        <section id="simulation" class="scroll-mt-20">
             <div class="text-center mb-12">
                <h3 class="text-3xl font-bold text-slate-900">Performance & Scalability Simulator</h3>
                <p class="mt-4 max-w-2xl mx-auto text-base text-slate-500">
                    How does the Rodial system perform under load? This simulation models the impact of an increasing number of connected devices on key network metrics like throughput and latency. Use the slider to adjust the device count and observe the real-time results on the chart.
                </p>
            </div>
            <div class="glassmorphism-card p-8">
                 <div class="mb-6">
                    <label for="device-slider" class="block font-medium text-slate-700">Number of Connected Devices: <span id="device-count" class="font-bold text-blue-600">3</span></label>
                    <input id="device-slider" type="range" min="0" max="4" value="0" step="1" class="w-full h-2 bg-slate-200 rounded-lg appearance-none cursor-pointer slider mt-2">
                </div>
                <div class="chart-container">
                    <canvas id="performanceChart"></canvas>
                </div>
                 <div id="performance-summary" class="mt-6 text-center text-slate-600 bg-slate-100 p-4 rounded-lg">
                    Summary will appear here.
                </div>
            </div>
        </section>
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const infoData = {
                'physical-router': {
                    title: 'Physical Router',
                    text: 'This is the origin point of the internet connection. It maintains a standard Wi-Fi network and provides the initial data seed (approx. 5MB) required to establish a connection with the Digital Twin. The ML model directs it to yield control of specific frequency bands upon command.'
                },
                'digital-twin': {
                    title: 'Digital Twin',
                    text: 'A cloud-based, exact replica of the physical home network. It receives the initial data seed and then takes over the data stream, managing traffic and routing it efficiently to the mobile device. It operates on the frequency band selected by the ML model for optimal performance.'
                },
                'mobile-device': {
                    title: 'Mobile Device with "Rodial" App',
                    text: 'The end-user device. The "Rodial" application establishes a secure, VPN-like tunnel to the Digital Twin, allowing the device to experience router-level internet speeds and latency, regardless of its geographical distance from the physical router.'
                }
            };

            const diagramNodes = document.querySelectorAll('.diagram-node');
            const infoBox = document.getElementById('info-box');
            const infoTitle = document.getElementById('info-title');
            const infoText = document.getElementById('info-text');

            diagramNodes.forEach(node => {
                node.addEventListener('click', () => {
                    const infoKey = node.getAttribute('data-info');
                    const data = infoData[infoKey];
                    
                    diagramNodes.forEach(n => n.classList.remove('ring-2', 'ring-blue-500'));
                    node.classList.add('ring-2', 'ring-blue-500');

                    infoBox.classList.remove('opacity-0');
                    infoTitle.textContent = data.title;
                    infoText.textContent = data.text;
                });
            });

            const bandButtons = document.querySelectorAll('.band-button');
            const mlStatus = document.getElementById('ml-status');
            let mlChart;

            const mlChartCtx = document.getElementById('mlChart').getContext('2d');
            
            function renderMlChart(selectedBandData) {
                 if (mlChart) {
                    mlChart.destroy();
                }
                mlChart = new Chart(mlChartCtx, {
                    type: 'bar',
                    data: {
                        labels: ['2.4 GHz', '5 GHz', '6 GHz'],
                        datasets: [{
                            label: 'ML Suitability Score',
                            data: selectedBandData,
                            backgroundColor: [
                                'rgba(59, 130, 246, 0.5)',
                                'rgba(37, 99, 235, 0.7)',
                                'rgba(29, 78, 216, 0.9)',
                            ],
                            borderColor: [
                                'rgba(59, 130, 246, 1)',
                                'rgba(37, 99, 235, 1)',
                                'rgba(29, 78, 216, 1)',
                            ],
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: false
                            },
                            tooltip: {
                                enabled: true
                            },
                            title: {
                                display: true,
                                text: 'ML Model Band Suitability Analysis'
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true,
                                max: 100,
                                title: {
                                    display: true,
                                    text: 'Suitability Score'
                                }
                            }
                        }
                    }
                });
            }
            renderMlChart([65, 80, 75]);

            bandButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const band = button.getAttribute('data-band');
                    bandButtons.forEach(btn => btn.classList.remove('active'));
                    button.classList.add('active');
                    mlStatus.textContent = `Digital Twin has taken control of the ${band} GHz band.`;

                    let scores = {
                        '2.4': [92, 60, 55],
                        '5': [70, 95, 85],
                        '6': [65, 88, 98]
                    };
                    renderMlChart(scores[band]);
                });
            });

            const deviceSlider = document.getElementById('device-slider');
            const deviceCount = document.getElementById('device-count');
            const performanceSummary = document.getElementById('performance-summary');
            let performanceChart;
            const deviceLevels = [3, 10, 50, 100, 1000];
            const performanceData = {
                throughput: [940, 925, 850, 780, 550],
                latency: [5, 7, 12, 18, 35],
                summaries: [
                    "Optimal performance. The system easily handles a few devices with maximum throughput and minimal latency.",
                    "Excellent performance. Minor increase in latency, throughput remains high. Ideal for most home use cases.",
                    "Good performance under moderate load. Throughput sees a noticeable dip, but the connection remains stable and fast.",
                    "Stable performance under heavy load. The system intelligently manages traffic, but latency increases and throughput is reduced.",
                    "Stressed performance at massive scale. The system prioritizes stability, resulting in significantly higher latency and lower throughput for each device."
                ]
            };

            const perfChartCtx = document.getElementById('performanceChart').getContext('2d');
            function renderPerformanceChart() {
                performanceChart = new Chart(perfChartCtx, {
                    type: 'line',
                    data: {
                        labels: deviceLevels,
                        datasets: [
                            {
                                label: 'Throughput (Mbps)',
                                data: performanceData.throughput,
                                borderColor: 'rgb(37, 99, 235)',
                                backgroundColor: 'rgba(37, 99, 235, 0.2)',
                                yAxisID: 'y',
                                tension: 0.1,
                                fill: true,
                            },
                            {
                                label: 'Latency (ms)',
                                data: performanceData.latency,
                                borderColor: 'rgb(234, 88, 12)',
                                backgroundColor: 'rgba(234, 88, 12, 0.2)',
                                yAxisID: 'y1',
                                tension: 0.1,
                                fill: false,
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        interaction: {
                            mode: 'index',
                            intersect: false,
                        },
                        plugins: {
                           tooltip: {
                                enabled: true
                            },
                        },
                        scales: {
                            x: {
                                title: {
                                    display: true,
                                    text: 'Number of Devices'
                                }
                            },
                            y: {
                                type: 'linear',
                                display: true,
                                position: 'left',
                                title: {
                                    display: true,
                                    text: 'Throughput (Mbps)'
                                },
                            },
                            y1: {
                                type: 'linear',
                                display: true,
                                position: 'right',
                                title: {
                                    display: true,
                                    text: 'Latency (ms)'
                                },
                                grid: {
                                    drawOnChartArea: false,
                                },
                            },
                        }
                    }
                });
            }
            renderPerformanceChart();
            
            function updatePerformanceView(index) {
                deviceCount.textContent = deviceLevels[index];
                performanceSummary.textContent = performanceData.summaries[index];
                
                if(performanceChart.tooltip) {
                    const chart = performanceChart;
                    const tooltip = chart.tooltip;
                    
                    const activeElements = [{
                        datasetIndex: 0,
                        index: index,
                    }, {
                        datasetIndex: 1,
                        index: index,
                    }];
                    
                    if (tooltip.getActiveElements().length > 0) {
                        tooltip.setActiveElements(activeElements, {});
                    } else {
                        tooltip.setActiveElements(activeElements, {});
                    }
                    chart.update();
                }
            }
            
            deviceSlider.addEventListener('input', (e) => {
                const index = parseInt(e.target.value);
                updatePerformanceView(index);
            });
            
            updatePerformanceView(0);

            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    document.querySelector(this.getAttribute('href')).scrollIntoView({
                        behavior: 'smooth'
                    });
                });
            });
        });
    </script>
</body>
</html>
